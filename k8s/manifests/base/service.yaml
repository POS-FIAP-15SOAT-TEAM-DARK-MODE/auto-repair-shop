apiVersion: v1
kind: Service
metadata:
  name: auto-repair-shop
  labels:
    app: auto-repair-shop
spec:
  type: ClusterIP
  selector:
    app: auto-repair-shop
  ports:
    - name: http
      port: 80
      targetPort: http
