apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Only for overlays where the Prometheus Operator CRDs exist (kube-prometheus-stack,
# installed by auto-repair-shop-infra-k8s): lab, stg, prd. Not local (Kind) —
# it has no addons at all, applying this there would fail on a missing CRD.
resources:
  - servicemonitor.yaml
